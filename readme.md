library vs framework
library - library is somthing which we can build from scratc
for example - react js , swiper js, gsap
framework - is structre or dhacha
for example - nextjs angular, express

What is react js
--> React js is javascript library used to build a single page application made by meta in 2013

import and export

export -> named ,
 default - sirf ek baar export karta hai kisi bhi element ko
 named - multiple chize import kar sakta hai 

real dom vs virtual dom

real dom -> is a actual dom
virtual dom -> copy of real dom

reconsiliton -> is process where a virtual dom compare/diffing with real dom and whatever changes occur in virtual dom it will re render that part in real dom


components
-> components is block of  code which we resuse whenever we need them
 
props drilling


Hooks
useState - for state management,
useEffect - side stack,
useRef - kisi bhi element ko select karne ke liye
useContext - global state ko manage karne ke liye
useReducer - complex chizo ko manage karne ke liye
useMemo,useCallback - memorization


react hook form - kyunki jab hum react me form handleling karne jaate the to code messay hojata tha this why we need


routing
react router dom



context api
->createContext()- data creation
->Providing the data
-->useContext() data use karne ke liye